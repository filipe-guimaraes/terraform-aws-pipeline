name: "PROD DEPLOY"

on:
  push:
    branches:
      - main

permissions:
      id-token: write   # This is required for requesting the JWT
      contents: read    # This is required for actions/checkout

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      environment: "prod"
      aws-assume-role-arn: "arn:aws:iam::701974052433:role/GitHubAction-AssumeRoleWithAction"
      aws-region: "us-east-1"
      aws-statefile-s3-bucket: "live-primeiroterraform-bucket"